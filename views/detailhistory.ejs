<div class="row" style="margin-top: 70px;width: 100%;text-align: right;padding-right: 20px;font-family: 'Open Sans', sans-serif;/* text-transform: uppercase; */letter-spacing: 3px;font-size: 2.2rem;">  
        <div class="col-md-4" style="text-align: left;padding-left: 30px;">
        </div>
        <div class="col-md-8"><div class="aa_htmlTable" style="padding: 1.3rem;">
  <table style="width: 100%" >
    <thead>
      <tr>
        <th>Kimden</th>
        <th>Mesaj</th>
        <th>Tarih</th>
      </tr>
    </thead>
    <tbody id="detailChatTable">
    <% if (typeof theChatRooms !== 'undefined' && theChatRooms.length > 0) { %>
          <% theChatRooms.forEach(function(data) { %>
            <tr id="<%- data.id %>">
              <td><img src="images/<%= data.from %>.png" style="width: 37px;"></td>
              <td><%- data.messages %></td>
              <td><%= data.time %></td>
           </tr>
          <% }) %>
    <% }else{ %>
          
    <% }%>

    </tbody>
    <tfoot>
    </tfoot>
  </table>
</div></div>
</div>





